import React, { useEffect, useState } from 'react';
import { Bell, ChevronDown, User, Settings, LogOut, CheckCheck } from 'lucide-react';
import { AccountRole } from '../../types';
import { BrandLogo } from './BrandLogo';
import { getCleanAvatarUrl } from '../../lib/avatarHelper';
import { supabase } from '../../lib/supabase';
import { AppNotification, markAllNotificationsRead, markNotificationRead, notificationTime, subscribeToNotifications } from '../../lib/notificationService';

interface LoggedInNavbarProps { currentPath: string; currentRole?: AccountRole; role?: AccountRole; pageTitle?: string; userName?: string; userAvatar?: string; onNavigate: (path: string) => void; onRoleChange?: (newRole: AccountRole) => void; onLogout?: () => void; }

const titles: Record<string,string> = {
  '/student/dashboard':'لوحة تحكم الطالب','/student/qr-card':'كارنيه الـ QR الرقمي','/student/tutors':'مدرسيني المسجلين','/student/book':'حجز حصة جديدة','/student/payments':'المدفوعات والإيصالات','/student/notifications':'الإشعارات والتنبيهات','/student/calendar':'التقويم الموحد','/student/messages':'الرسائل','/student/assignments':'الواجبات والملفات','/student/grades':'الدرجات والتقارير','/student/attendance':'سجل الحضور','/student/reviews':'تقييم المدرسين',
  '/parent/dashboard':'لوحة متابعة ولي الأمر','/parent/attendance':'سجل الحضور اللحظي','/parent/payments':'المدفوعات والمصروفات','/parent/settings':'إعدادات الإشعارات والواتساب','/parent/notifications':'الإشعارات والتنبيهات','/parent/calendar':'التقويم الموحد','/parent/messages':'الرسائل','/parent/grades':'درجات الأبناء وتقاريرهم',
  '/teacher/dashboard':'لوحة تحكم المعلم','/teacher/students':'دليل الطلاب والاشتراكات','/teacher/groups':'إدارة المجموعات والسناتر','/teacher/scan':'ماسح حضور الـ QR','/teacher/attendance':'إدارة الحضور المتطور','/teacher/payments':'الأرباح والعمولة','/teacher/availability':'إدارة المواعيد المتاحة','/teacher/profile':'تعديل البروفايل العام','/teacher/reviews':'تقييمات وآراء الطلاب','/teacher/notifications':'الإشعارات والتنبيهات','/teacher/calendar':'التقويم الموحد','/teacher/messages':'الرسائل','/teacher/assignments':'الواجبات والتصحيح',
};

const roleLabels: Record<AccountRole,{title:string;badge:string;path:string;fallback:string}> = {
  student:{title:'حساب طالب',badge:'bg-blue-50 text-blue-700 border-blue-200',path:'/student/profile',fallback:'طالب حِصّتي'},
  parent:{title:'حساب ولي أمر',badge:'bg-amber-50 text-amber-800 border-amber-200',path:'/parent/settings',fallback:'ولي أمر حِصّتي'},
  teacher:{title:'حساب مدرس',badge:'bg-emerald-50 text-emerald-800 border-emerald-200',path:'/teacher/profile',fallback:'معلم حِصّتي'},
  assistant:{title:'حساب مساعد',badge:'bg-teal-50 text-teal-800 border-teal-200',path:'/teacher/dashboard',fallback:'مساعد حِصّتي'},
  admin:{title:'حساب إدارة',badge:'bg-purple-50 text-purple-800 border-purple-200',path:'/admin',fallback:'مدير حِصّتي'},
};

export const LoggedInNavbar: React.FC<LoggedInNavbarProps> = ({currentPath,currentRole,role:propRole,pageTitle, userName:propUserName,userAvatar:propUserAvatar,onNavigate,onRoleChange,onLogout}) => {
  const role=propRole||currentRole||'student'; const meta=roleLabels[role]||roleLabels.student; const userName=propUserName||meta.fallback; const userAvatar=getCleanAvatarUrl(propUserAvatar,role,userName);
  const [notifications,setNotifications]=useState<AppNotification[]>([]); const [open,setOpen]=useState(false); const [userMenu,setUserMenu]=useState(false); const [userId,setUserId]=useState<string|null>(null);
  useEffect(()=>{let active=true;let unsubscribe=()=>{};(async()=>{if(!supabase)return;const {data}=await supabase.auth.getUser();if(!active||!data.user)return;setUserId(data.user.id);unsubscribe=subscribeToNotifications(data.user.id,setNotifications,(error)=>console.warn('Notifications load:',error));})();return()=>{active=false;unsubscribe();};},[]);
  const unread=notifications.filter(n=>!n.read_at).length;
  const openNotification=async(item:AppNotification)=>{try{if(!item.read_at)await markNotificationRead(item.id);}catch(e){console.warn('Mark notification read:',e);}setOpen(false);if(item.link)onNavigate(item.link);};
  const markAllRead=async()=>{if(!userId||unread===0)return;try{await markAllNotificationsRead(userId);}catch(e){console.warn('Mark all notifications read:',e);}};
  const switchRole=(next:AccountRole)=>{if(onRoleChange)onRoleChange(next);else if(next!=='admin')onNavigate(`/${next}/dashboard`);};
  const title=pageTitle||titles[currentPath]||'لوحة التحكم';
  return <header className="sticky top-0 z-40 bg-white border-b border-[#E5E7EB]"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div className="flex items-center justify-between min-h-[72px] gap-3"><div className="flex items-center gap-3 sm:gap-5 min-w-0"><button onClick={()=>onNavigate('/')} className="shrink-0 cursor-pointer" aria-label="الرئيسية"><BrandLogo size="sm" showSubtitle={false}/></button><div className="h-6 w-px bg-gray-200 hidden sm:block"/><h1 className="text-sm sm:text-lg font-bold text-[#1E3A8A] truncate">{title}</h1></div><div className="flex items-center gap-2 sm:gap-3"><div className="hidden lg:flex items-center bg-gray-50 border border-gray-200 rounded-xl p-1 gap-1 text-xs font-bold">{(['student','parent','teacher'] as AccountRole[]).map(r=><button key={r} onClick={()=>switchRole(r)} className={`px-2.5 py-1.5 rounded-lg ${role===r?'bg-blue-600 text-white':'text-gray-600 hover:text-blue-600'}`}>{r==='student'?'طالب':r==='parent'?'ولي أمر':'مدرس'}</button>)}</div><div className="relative"><button onClick={()=>{setOpen(v=>!v);setUserMenu(false);}} className="relative p-2.5 rounded-xl border border-gray-200 text-gray-600 hover:text-blue-600 hover:bg-gray-50" aria-label="الإشعارات"><Bell className="w-5 h-5"/>{unread>0&&<span className="absolute -top-1 -right-1 min-w-5 h-5 px-1 bg-red-500 text-white text-[10px] font-black rounded-full flex items-center justify-center ring-2 ring-white">{unread>99?'99+':unread}</span>}</button>{open&&<div className="absolute left-0 mt-2 w-[calc(100vw-2rem)] sm:w-96 max-w-96 bg-white border border-gray-200 rounded-2xl shadow-2xl overflow-hidden z-50 text-right"><div className="p-4 flex items-center justify-between border-b border-gray-100"><div><h3 className="text-sm font-black text-[#1E3A8A]">الإشعارات والتنبيهات</h3><p className="text-[10px] text-gray-400 mt-0.5">تحديثات مباشرة من المنصة</p></div>{unread>0&&<button onClick={markAllRead} className="text-[11px] font-bold text-blue-600 hover:underline flex items-center gap-1"><CheckCheck className="w-3.5 h-3.5"/>تحديد الكل كمقروء</button>}</div><div className="max-h-[65vh] overflow-y-auto">{notifications.length===0?<div className="p-10 text-center text-xs text-gray-400">لا توجد إشعارات حتى الآن 🔔</div>:notifications.map(item=><button key={item.id} onClick={()=>openNotification(item)} className={`w-full text-right p-4 border-b border-gray-50 hover:bg-gray-50 transition ${!item.read_at?'bg-blue-50/50':'bg-white'}`}><div className="flex items-start gap-3"><div className={`w-9 h-9 rounded-xl shrink-0 flex items-center justify-center ${item.type==='attendance'?'bg-emerald-50 text-emerald-600':item.type==='payment'?'bg-violet-50 text-violet-600':item.type==='booking'?'bg-blue-50 text-blue-600':item.type==='support'?'bg-amber-50 text-amber-600':'bg-gray-100 text-gray-600'}`}><Bell className="w-4 h-4"/></div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><h4 className="text-xs font-black text-gray-800">{item.title}</h4><span className="text-[10px] text-gray-400 shrink-0">{notificationTime(item.created_at)}</span></div><p className="text-[11px] text-gray-500 mt-1 leading-5">{item.message}</p>{!item.read_at&&<span className="inline-block mt-2 w-2 h-2 rounded-full bg-blue-600"/>}</div></div></button>)}</div></div>}</div><div className="relative"><button onClick={()=>{setUserMenu(v=>!v);setOpen(false);}} className="flex items-center gap-2 p-1.5 sm:px-3 rounded-xl border border-gray-200 hover:border-blue-300"><img src={userAvatar} alt={userName} className="w-8 h-8 rounded-lg object-cover border border-gray-200" referrerPolicy="no-referrer"/><div className="hidden sm:flex flex-col text-right"><span className="text-xs font-bold text-gray-800 max-w-[120px] truncate">{userName}</span><span className="text-[10px] text-blue-600 font-semibold">{meta.title}</span></div><ChevronDown className="w-4 h-4 text-gray-400"/></button>{userMenu&&<div className="absolute left-0 mt-2 w-56 bg-white border border-gray-200 rounded-2xl shadow-xl p-2 z-50 text-right"><button onClick={()=>{onNavigate(meta.path);setUserMenu(false);}} className="w-full text-right px-3 py-2.5 text-xs font-bold text-gray-700 hover:bg-blue-50 rounded-xl flex items-center gap-2"><User className="w-4 h-4 text-gray-400"/>الملف الشخصي</button>{role==='parent'&&<button onClick={()=>{onNavigate('/parent/settings');setUserMenu(false);}} className="w-full text-right px-3 py-2.5 text-xs font-bold text-gray-700 hover:bg-blue-50 rounded-xl flex items-center gap-2"><Settings className="w-4 h-4 text-gray-400"/>إعدادات الإشعارات</button>}{role==='teacher'&&<button onClick={()=>{onNavigate('/teacher/profile');setUserMenu(false);}} className="w-full text-right px-3 py-2.5 text-xs font-bold text-gray-700 hover:bg-blue-50 rounded-xl flex items-center gap-2"><Settings className="w-4 h-4 text-gray-400"/>تعديل البروفايل</button>}<div className="mt-1 pt-1 border-t border-gray-100"><button onClick={()=>{setUserMenu(false);onLogout?.();}} className="w-full text-right px-3 py-2.5 text-xs font-bold text-red-600 hover:bg-red-50 rounded-xl flex items-center gap-2"><LogOut className="w-4 h-4"/>تسجيل الخروج</button></div></div>}</div></div></div></div></header>;
};
