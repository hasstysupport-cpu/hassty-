import React,{useEffect,useMemo,useState}from'react';import{DollarSign,RefreshCw,Loader2,AlertCircle,TrendingUp}from'lucide-react';import{useAuth}from'../../lib/AuthContext';import{supabase}from'../../lib/supabase';import{Badge}from'../../components/common/Badge';
export const TeacherPaymentsPageV2:React.FC<{onNavigate?:(path:string)=>void}>=({onNavigate})=>{const{user}=useAuth();const[rows,setRows]=useState<any[]>([]);const[students,setStudents]=useState(0);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const load=async()=>{if(!supabase||!user?.uid){setLoading(false);return;}setLoading(true);setError('');const{data:r,error:e}=await supabase.from('commission_tracking').select('id,billing_cycle,active_students_count,monthly_gross_egp,tier_rate,due_commission_egp,payment_status,last_payment_date,created_at').eq('teacher_id',user.uid).order('billing_cycle',{ascending:false}).limit(24);if(e){setError('تعذر تحميل العمولات من Supabase.');setLoading(false);return;}const{count}=await supabase.from('group_enrollments').select('id',{count:'exact',head:true}).eq('status','active');setRows(r||[]);setStudents(count||0);setLoading(false)};useEffect(()=>{void load()},[user?.uid]);useEffect(()=>{if(!supabase||!user?.uid)return;const ch=supabase.channel(`teacher-commissions-${user.uid}`).on('postgres_changes',{event:'*',schema:'public',table:'commission_tracking',filter:`teacher_id=eq.${user.uid}`},()=>void load()).subscribe();return()=>{void supabase.removeChannel(ch)}},[user?.uid]);const latest=rows[0];const gross=Number(latest?.monthly_gross_egp||0);const due=Number(latest?.due_commission_egp||0);const paid=rows.filter(r=>r.payment_status==='paid').reduce((s,r)=>s+Number(r.due_commission_egp||0),0);const rate=Number(latest?.tier_rate||0);const status=(latest?.payment_status||'pending') as string;const statusText=status==='paid'?'تم السداد':status==='overdue'?'متأخر':'قيد التحصيل';return <div className="space-y-6 text-right" dir="rtl"><section className="bg-white border border-slate-200 rounded-3xl p-6 flex justify-between"><div><div className="text-xs font-black text-blue-700 flex gap-2"><DollarSign className="w-4 h-4"/>المستحقات المالية</div><h1 className="text-2xl font-black mt-2">أرباحك وعمولة المنصة</h1><p className="text-xs text-slate-500 mt-1">الأرقام مأخوذة من سجلات Supabase، بدون تقديرات تجريبية.</p></div><button onClick={()=>void load()} className="rounded-xl border px-3 py-2 text-xs font-bold flex gap-2"><RefreshCw className={loading?'animate-spin':''}/>تحديث</button></section>{error&&<div className="rounded-2xl border border-red-200 bg-red-50 p-3 text-xs font-bold text-red-800 flex gap-2"><AlertCircle className="w-4 h-4"/>{error}</div>}<section className="grid grid-cols-2 lg:grid-cols-4 gap-3"><Stat label="الطلاب النشطون" value={students}/><Stat label="إجمالي الشهر" value={`${gross.toLocaleString('ar-EG')} ج.م`}/><Stat label="عمولة المنصة" value={`${due.toLocaleString('ar-EG')} ج.م`} /><Stat label="نسبة العمولة" value={`${rate}%`}/></section><section className="bg-white border rounded-3xl p-5"><div className="flex items-center gap-2 font-black mb-4"><TrendingUp className="w-5 h-5 text-blue-600"/>الدورات المالية</div>{loading?<div className="py-12 text-center"><Loader2 className="mx-auto animate-spin text-blue-600"/></div>:rows.length===0?<div className="py-12 text-center text-slate-500"><DollarSign className="mx-auto mb-2 text-slate-300"/><p className="font-bold">لا توجد دورة عمولة مسجلة بعد</p><p className="text-xs mt-1">سيظهر السجل عند إنشاء دورة مالية حقيقية.</p></div>:<div className="overflow-x-auto"><table className="w-full text-right"><thead><tr className="border-b text-xs text-slate-500"><th className="p-3">الدورة</th><th className="p-3">الطلاب</th><th className="p-3">الإجمالي</th><th className="p-3">العمولة</th><th className="p-3">الحالة</th></tr></thead><tbody className="divide-y">{rows.map(r=><tr key={r.id} className="text-xs"><td className="p-3">{new Date(r.billing_cycle).toLocaleDateString('ar-EG',{month:'long',year:'numeric'})}</td><td className="p-3 font-bold">{r.active_students_count}</td><td className="p-3">{Number(r.monthly_gross_egp).toLocaleString('ar-EG')} ج.م</td><td className="p-3 font-black">{Number(r.due_commission_egp).toLocaleString('ar-EG')} ج.م</td><td className="p-3"><Badge variant={r.payment_status==='paid'?'success':r.payment_status==='overdue'?'error':'warning'} size="sm">{r.payment_status==='paid'?'تم السداد':r.payment_status==='overdue'?'متأخر':'قيد التحصيل'}</Badge></td></tr>)}</tbody></table></div>}</section><div className="text-xs text-slate-500">إجمالي العمولات المسددة تاريخيًا: <strong className="text-slate-900">{paid.toLocaleString('ar-EG')} ج.م</strong></div></div>};
const Stat=({label,value}:{label:string;value:string|number})=><div className="bg-white border rounded-2xl p-4"><div className="text-xs text-slate-500 font-bold">{label}</div><div className="text-2xl font-black mt-2">{value}</div></div>;
