import React, { useState } from 'react';
import { BriefcaseBusiness, CheckCircle2, FileBadge, MapPin, ShieldCheck, User, Eye, EyeOff } from 'lucide-react';
import { supabase } from '../lib/supabase';
import { LocationSelector } from '../components/common/LocationSelector';

export const AssistantSignupPage: React.FC<{ onNavigate: (path: string) => void }> = ({ onNavigate }) => {
  const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [showPassword, setShowPassword] = useState(false);
  const [phone, setPhone] = useState(''); const [whatsappPhone, setWhatsappPhone] = useState(''); const [governorate, setGovernorate] = useState('القاهرة'); const [city, setCity] = useState('مدينة نصر');
  const [experienceYears, setExperienceYears] = useState(''); const [experienceSummary, setExperienceSummary] = useState(''); const [education, setEducation] = useState(''); const [certificateSummary, setCertificateSummary] = useState('');
  const [agreed, setAgreed] = useState(false); const [loading, setLoading] = useState(false); const [success, setSuccess] = useState(false); const [error, setError] = useState('');

  const submit = async (e: React.FormEvent) => {
    e.preventDefault(); setError('');
    if (!agreed) return setError('يجب الموافقة على شروط التسجيل كمساعد قبل إرسال الطلب.');
    if (!supabase) return setError('تعذر الاتصال بقاعدة البيانات.');
    if (!name.trim() || !email.trim() || !password || !phone.trim() || !whatsappPhone.trim() || !governorate || !city) return setError('أكمل جميع البيانات الأساسية.');
    if (password.length < 8) return setError('كلمة المرور يجب أن تكون 8 أحرف أو أكثر.');
    setLoading(true);
    try {
      const { data: authData, error: authError } = await supabase.auth.signUp({ email: email.trim().toLowerCase(), password, options: { data: { full_name: name.trim(), role: 'assistant' }, emailRedirectTo: `${window.location.origin}/verify-email` } });
      if (authError) throw authError; if (!authData.user) throw new Error('لم يتم إنشاء الحساب.');
      const uid = authData.user.id;
      const { error: profileError } = await supabase.from('profiles').upsert({ id: uid, email: email.trim().toLowerCase(), full_name: name.trim(), phone: phone.trim(), role: 'assistant', governorate, city, account_status: 'under_review', badge: 'none', metadata: { authProvider: 'email', onboardingComplete: true, isVerified: false, verificationStatus: 'pending' } }, { onConflict: 'id' });
      if (profileError) throw profileError;
      const { error: assistantError } = await supabase.from('assistant_profiles').upsert({ user_id: uid, full_name: name.trim(), phone: phone.trim(), whatsapp_phone: whatsappPhone.trim(), governorate, city, experience_years: Number(experienceYears) || 0, experience_summary: experienceSummary.trim(), education: education.trim(), certificate_summary: certificateSummary.trim(), verification_status: 'pending', is_verified: false }, { onConflict: 'user_id' });
      if (assistantError) throw assistantError;
      const { error: requestError } = await supabase.from('assistant_verification_requests').insert({ assistant_id: uid, status: 'pending' });
      if (requestError) throw requestError;
      await supabase.from('notifications').insert({ user_id: uid, title: 'تم استلام طلب المساعد', message: 'تم استلام طلب انضمامك كمساعد. سيقوم فريق حِصّتي بالتواصل معك لاستكمال توثيق الهوية والمؤهلات.', type: 'verification', link: '/assistant/verification' });
      await supabase.auth.signOut(); setSuccess(true);
    } catch (err: any) { setError(err?.message || 'حدث خطأ أثناء إرسال الطلب.'); }
    finally { setLoading(false); }
  };

  if (success) return <div className="min-h-screen bg-[#F8FAFF] flex items-center justify-center p-5 font-['IBM_Plex_Sans_Arabic',sans-serif]" dir="rtl"><div className="w-full max-w-xl bg-white border border-slate-200 rounded-3xl p-8 text-center shadow-sm"><div className="mx-auto w-16 h-16 rounded-2xl bg-emerald-50 text-emerald-600 flex items-center justify-center mb-5"><CheckCircle2 className="w-8 h-8" /></div><h1 className="text-2xl font-black text-[#1E3A8A]">تم إرسال طلبك بنجاح ✅</h1><p className="mt-3 text-sm text-slate-600 leading-7">تم تسجيل طلبك كمساعد وهو الآن تحت المراجعة. سيطلب منك فريق الإدارة بطاقة الهوية والمؤهلات عند بدء التوثيق، وسيتم التواصل معك عبر واتساب.</p><button onClick={() => onNavigate('/')} className="mt-7 px-6 py-3 rounded-2xl bg-blue-600 text-white font-black">العودة للموقع</button></div></div>;

  return <div className="min-h-screen bg-[#F8FAFF] py-10 px-4 font-['IBM_Plex_Sans_Arabic',sans-serif]" dir="rtl"><div className="max-w-3xl mx-auto"><button onClick={() => onNavigate('/')} className="text-sm font-bold text-blue-600 mb-5">← العودة للموقع</button><div className="bg-white border border-slate-200 rounded-3xl shadow-sm overflow-hidden"><div className="p-7 border-b border-slate-100 bg-gradient-to-br from-blue-50 to-indigo-50"><div className="flex items-center gap-3"><div className="w-12 h-12 rounded-2xl bg-blue-600 text-white flex items-center justify-center"><BriefcaseBusiness className="w-6 h-6" /></div><div><h1 className="text-2xl font-black text-[#1E3A8A]">التسجيل كمساعد مدرس</h1><p className="text-sm text-slate-600 mt-1">بياناتك أولًا، ثم مراجعة وتوثيق الإدارة.</p></div></div></div><form onSubmit={submit} className="p-7 space-y-7"><section className="space-y-4"><h2 className="font-black text-[#1E3A8A] flex items-center gap-2"><User className="w-5 h-5 text-blue-600" /> البيانات الأساسية</h2><div className="grid md:grid-cols-2 gap-4"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="الاسم بالكامل" className="w-full p-3.5 rounded-xl border border-slate-200"/><input required dir="ltr" type="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="رقم الهاتف" className="w-full p-3.5 rounded-xl border border-slate-200"/><input required dir="ltr" type="tel" value={whatsappPhone} onChange={e=>setWhatsappPhone(e.target.value)} placeholder="رقم واتساب للتواصل" className="w-full p-3.5 rounded-xl border border-slate-200"/><input required dir="ltr" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="البريد الإلكتروني" className="w-full p-3.5 rounded-xl border border-slate-200"/><div className="relative"><input required dir="ltr" minLength={8} type={showPassword?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="كلمة مرور قوية" className="w-full p-3.5 pl-11 rounded-xl border border-slate-200"/><button type="button" onClick={()=>setShowPassword(v=>!v)} className="absolute left-3 top-3.5 text-slate-400">{showPassword?<EyeOff className="w-5 h-5"/>:<Eye className="w-5 h-5"/>}</button></div></div></section><section className="space-y-4"><h2 className="font-black text-[#1E3A8A] flex items-center gap-2"><MapPin className="w-5 h-5 text-blue-600" /> المكان والخبرة</h2><LocationSelector selectedGovernorate={governorate} selectedCity={city} onSelectGovernorate={setGovernorate} onSelectCity={setCity} showCitySelect placeholder="اختر المحافظة والمدينة"/><div className="grid md:grid-cols-2 gap-4"><input type="number" min="0" max="60" value={experienceYears} onChange={e=>setExperienceYears(e.target.value)} placeholder="سنوات الخبرة" className="w-full p-3.5 rounded-xl border border-slate-200"/><input value={education} onChange={e=>setEducation(e.target.value)} placeholder="المؤهل الدراسي" className="w-full p-3.5 rounded-xl border border-slate-200"/></div><textarea value={experienceSummary} onChange={e=>setExperienceSummary(e.target.value)} placeholder="الخبرات والمهام السابقة" rows={4} className="w-full p-3.5 rounded-xl border border-slate-200 resize-none"/><textarea value={certificateSummary} onChange={e=>setCertificateSummary(e.target.value)} placeholder="الشهادات والدورات" rows={3} className="w-full p-3.5 rounded-xl border border-slate-200 resize-none"/></section><section className="p-4 rounded-2xl bg-amber-50 border border-amber-200"><div className="flex items-start gap-3"><FileBadge className="w-5 h-5 text-amber-600 mt-0.5"/><div className="text-sm text-amber-900 leading-6"><strong>التوثيق:</strong> بعد إرسال الطلب يظهر الملف للأدمن. قد يتم طلب صورة البطاقة والمؤهلات وبيانات إضافية قبل الاعتماد النهائي.</div></div></section><label className="flex items-start gap-3 p-4 rounded-2xl border border-slate-200 cursor-pointer"><input type="checkbox" checked={agreed} onChange={e=>setAgreed(e.target.checked)} className="mt-1 w-4 h-4"/><span className="text-sm text-slate-700 leading-6">أوافق على شروط المساعدين، وأتعهد بصحة بياناتي وأوافق على التحقق من هويتي ومؤهلاتي بواسطة إدارة حِصّتي.</span></label>{error && <div className="p-3.5 rounded-2xl bg-red-50 border border-red-200 text-red-700 text-sm font-bold">{error}</div>}<button disabled={loading} type="submit" className="w-full py-4 rounded-2xl bg-[#2563EB] text-white font-black flex items-center justify-center gap-2 disabled:opacity-50"><ShieldCheck className="w-5 h-5"/>{loading?'جاري إرسال الطلب...':'إرسال طلب التسجيل والتوثيق'}</button><p className="text-center text-xs text-slate-400">إنشاء الحساب لا يمنح أي صلاحيات مدرس حتى تعتمد الإدارة الطلب.</p></form></div></div></div>;
};
