rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all users during setup and production application use
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
